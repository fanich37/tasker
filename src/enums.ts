export enum TaskType {
  Develop = 'develop',
  Bug = 'bug',
  Analisys = 'analysis',
}

export enum TaskStatus {
  ToDo = 'todo',
  InProgress = 'inProgress',
  Paused = 'paused',
  Done = 'done',
}
